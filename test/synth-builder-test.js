var assert = require('assert');
describe("synth builder tests",()=>{
  it("is constructed with name of synth-def",()=>{
    assert(false);
  });
  it("calls createSynth on server when new is called and returns a promise for the synth instance",()=>{
    assert(false);
  });
});
